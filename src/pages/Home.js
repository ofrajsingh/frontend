import React from "react";

const Home = () => {
  const containerStyle = {
    backgroundColor: "#866ace",
    margin: 0,
    padding: 0, 
    width: '100vw',
    height: '100vh',
    overflow: 'hidden'
  };

  return <div style={containerStyle}>HOME</div>;
};

export default Home;
